# `modules/celloracle/gradient_dev/`

Single-process module `CO_GRADIENT_DEV`. Builds the reference
differentiation gradient and the `Oracle_development_module`, and
renders the headline inner-product plot.

See `../../../docs/steps/05_gradient_dev.md`.
